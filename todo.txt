
- AGA: 16+16 dual playfield
- ECS: 16 colors, sprites for the 4 first elevators, then bobs

reverse stuff:

- when color changes (light shot)
- colorbanks (D506/7)

enemy A.I when is 51A7 calles?

jump_table_0BBF

end of mainloop
76B1: CD 68 0B    call $0B68
76B4: CD E8 2F    call $2FE8
76B7: CD 27 31    call $3127
76BA: CD 81 30    call $3081
76BD: CD BA 4B    call $4BBA
76C0: CD E1 0B    call $0BE1
76C3: CD A0 15    call $15A0

  
figure 800E pointers? values?



0e9f called when??? reset charset???


- player crushed by elevator: working!

TODO:

- elevator dynamic colors: in progress

elevator column scroll:
- if brutal scroll change, just reset all colors to green for that elevator
- when writing a elevator floor tile:
  * 3a: write full elevator cabin (clipped)
- when small scroll change:
  * check positions of 3a + 3e
    on 3a: draw 8 green + X pink (clipped)
    on 3e: draw 8 pink + 1 green (clipped)
   implemented, not selecting proper column!
   
ISSUES:

- main font color in title: yellow???
- enemy behaviour & sprites completely wrong
- sprite right clipping
- jump into elevator animation going too far (killed frames appear) LOG_REGS remain check!!!
  when accessing sprite_code_table_62E2
- doors open: only red doors (special case to make with color attribute on doors)
- reaching bottom floor: goes back too far (higher door detect probably not working)
- wrong colors on sprite which hides character when taking stairs
- head of character remains displayed at the end of stairs
- opening doors remain displayed
- sprites completely wrong when player takes the document	
- NTSC version: remove brick wall, bottom status higher

to debug:
	.ifndef	RELEASE
	move.w	#0xC740,d7
	jbsr	display_debug_value
	addq.w	#3,d7
	exg	d0,d2
	jbsr	display_debug_value
	exg	d0,d2
	move.w	d0,-(a7)
	addq.w	#3,d7
	move.b	bottom_floor_horizon_8003(a6),d0
	jbsr	display_debug_value
	addq.w	#3,d7
	move.b	bottom_floor_fine_horizon_8002(a6),d0
	jbsr	display_debug_value
	move.w	(a7)+,d0
	.endif
298A <= state:coin screen**title_screen_16bit_timer_836F increases and switches screen at 7188

DK check issues????

